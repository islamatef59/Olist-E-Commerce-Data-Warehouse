# Olist-E-Commerce-Data-Warehouse
Built an automated ETL pipeline with Apache Airflow, Python (Pandas), and PostgreSQL to process the Olist Brazilian E-Commerce dataset. The pipeline cleans and transforms raw data, builds a Star Schema, loads it into PostgreSQL, and exports the processed data to CSV.
